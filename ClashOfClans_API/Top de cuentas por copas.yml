info:
  name: Top de cuentas por copas
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:2505/stats/top?n=5
  params:
    - name: n
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
